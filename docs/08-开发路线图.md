# 开发路线图

## 已完成 (v0.1) — 基础设施与核心引擎

### common/ — 公共基础设施

- [x] 配置加载：4 层 LLM 配置合并（平台级 -> Smith 出厂级 -> Agent 级 -> 会话级）
- [x] SQLite WAL 模式：6 张表
- [x] 文件系统操作工具
- [x] 日志系统
- [x] YAML 工具

### engine/ — 自研 Agent 框架

**执行引擎**
- [x] 任务路由：3 条路由（直接回复 / 技能链 / ReAct）
- [x] 技能链 DAG：Feature / BugFix 两条内置链
- [x] ReAct 循环：同步 + 流式双模式
- [x] 12 个门禁（Gate）
- [x] 回退机制 + `FailureLoopGuard`（`max_same=2` / `max_strategies=2`）

**LLM 客户端**
- [x] OpenAI 兼容协议
- [x] 流式输出
- [x] `tool_call` 支持
- [x] 3 次自动重试

**Prompt 组装**
- [x] 11 层 Prompt 组装（role / style / workflow / toolbox / context / memory / skills / safety / ...）

**工具系统**
- [x] 工具协议 + 注册机制
- [x] JSON Schema 自动生成

**技能系统**
- [x] 技能加载 + 注册 + 执行
- [x] 版本管理（保留 10 个版本）

**记忆系统**
- [x] agent / project 双作用域存储
- [x] Dream 整理：秘密过滤（8 regex） + 30 天剪枝 + 70% 关键词去重 + 模式提取

**用户偏好学习**
- [x] 4 维启发式检测（language / verbosity / tech_level / ...）
- [x] 3 次观察确认阈值

**插件系统**
- [x] Webhook + Polling 两种触发模式
- [x] 动态加载 handler

**安全护栏**
- [x] 24 条规则，7 大分类

### agents/ — Agent 内容定义

- [x] 单 Smith 身份种子（role.md / style.md / workflow.md / toolbox.md / context.md / config.yaml）+ skill 化能力扩展
- [x] 18 个内置技能
- [x] 14 个工具 provider
- [x] GitHub webhook 插件
- [x] 安全规则定义

### server/ — FastAPI 后端

- [x] DDD 分层架构：domain / services / repositories / routers
- [x] 23+ API 端点
- [x] Agent CRUD
- [x] 会话管理：同步 + SSE 流式
- [x] 任务管理
- [x] 自动任务：cron / interval / manual
- [x] 兼容模板接口（返回 Smith 的 `personal-assistant` id）
- [x] 文件管理
- [x] LLM 配置管理
- [x] 统计面板
- [x] 插件管理
- [x] 团队群聊：`@mention` 路由

### shell/ — Ink 终端前端

- [x] Ink/React 终端工作台
- [x] 本地 server 自动启动
- [x] 会话恢复与 SSE 流式渲染
- [x] slash commands
- [x] 插件 / 技能面板
- [ ] Biome 质量检查继续收敛

### macOS 原生前端

- [ ] 已暂缓；`app/` 不再作为当前工作树主线目录

### 验证

- [x] GLM-4.7 真实回复验证通过
- [x] 3-Agent Waves e2e 验证通过

---

## v0.2 — shell/server 打磨

> 目标：围绕 `shell/` 打磨真实后端 API、流式交互和 Smith 单身份体验。

- [ ] `shell/src/api.ts` 与后端 OpenAPI 保持兼容
- [ ] Smith 创建从 `agents/smith/` 身份种子初始化
- [ ] 会话消息真实 SSE 流式
- [ ] 自动任务 shell 入口
- [ ] 插件、技能、记忆状态继续补齐真实 API 展示

---

## v0.3 — 能力增强

> 目标：提升 Agent 回复质量与系统扩展能力。

- [ ] 记忆编译管线（LLM 摘要蒸馏，4 层编译：today / week / longterm / facts）
- [ ] EventBus 索引订阅（解耦组件通信）
- [ ] 插件二级权限模型（`trust_level` + `CAPABILITY_TABLE`）
- [ ] 向量搜索（替代关键词搜索）

---

## v0.4 — 协作与扩展

> 目标：多 Agent 并行协作 + 技能生态 + 外部 IM 触达。

- [ ] Sub-Agent 异步委派（并行执行，per-session 上限 5，全局上限 10）
- [ ] 技能市场（安装 / 卸载 / 评分）
- [ ] IM 桥接（飞书 + 钉钉适配器）

---

## v0.5 — 打磨与交付

> 目标：知识集成 + 高级协作 + 模板生态。

- [ ] 知识库集成（Hub MCP 连接）
- [ ] 多 Agent 圆桌会议
- [ ] 角色模板导入导出
