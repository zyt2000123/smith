# 02 · 系统架构

## 核心设计哲学

**宏观确定性控制，微观 LLM 自由度。**

代码（规则引擎、DAG、门禁）管流程，LLM 只在 ReAct 循环里获得「做事」的自由。这避免了纯 LLM Agent 的不可控问题。

运行定位上，Agent-Smith 是 **terminal-native / shell-native** 的本地进程 Agent。`shell` 是一等执行能力；`read_file`、`write_file`、`grep`、`git_ops`、`web_fetch` 等结构化工具，是对 shell/file/git/web 高频操作的安全封装。安全边界按能力划分（文件、网络、Git 副作用、删除、写入、secret 访问），而不是按工具名粗暴区分。

> 过渡说明：产品定位已经收口为“个人助手 Agent”，但当前 server / app / SQLite / 运行时目录里仍保留 `employee` / `employees` 命名，以兼容现有前端和数据模型。这是刻意保留的实现壳，与产品对外定位无关。

## 一、五层分离架构

```
app/ ── HTTP/SSE ──→ server/ ── import ──→ engine/ ── import ──→ common/
                              │                      ↑
                              └──── 读取 ────→ agents/ ┘
```

依赖方向单向不可逆。`common` 在最底层不依赖任何其他层。`agents/` 是纯内容，被 engine 加载。

| 层 | 目录 | 职责 | 语言 | 关键约束 |
|---|---|---|---|---|
| 基础设施 | `common/` | 配置、数据库、文件系统、日志 | Python | 零业务逻辑，不依赖任何其他层 |
| 执行框架 | `engine/` | 自研 Agent 框架（LLM 调用、执行引擎、记忆、安全） | Python | 不知道 FastAPI、不知道「Agent 实例管理」概念 |
| 内容定义 | `agents/` | Agent 模板、内置技能、内置工具、安全规则 | .md / .yaml / .json + Python | 新加 Agent 只碰这里 |
| 平台后端 | `server/` | FastAPI（Agent 实例管理、会话、任务、API） | Python | 薄壳路由 → 服务编排 → Repository 持久化 |
| 原生前端 | `app/` | SwiftUI macOS 原生应用 | Swift | 通过 HTTP/SSE 与 server 通信 |

**分层测试**：把 FastAPI 换成 Flask，只需重写 `server/`——engine、agents、common 完全不动。把 macOS 前端换成 Web 前端，也只改前端层。

---

## 二、各层详细职责

### 2.1 common/ — 公共基础设施

零业务逻辑。只放路径约定、数据库连接、配置合并、日志、文件工具。

```
common/
├── config.py            # 路径常量
├── config_loader.py     # 四层 LLM 配置合并（环境变量 → 平台级 → 模板级 → Agent 级 → 会话级）
├── database.py          # SQLite WAL + 6 表 schema
├── filesystem.py        # Agent 目录操作（~/.agent-smith/ 结构）
├── log.py               # 统一日志
├── yaml_utils.py        # YAML / JSON 读写
└── pyproject.toml       # 依赖: pyyaml, aiosqlite
```

**路径常量**（`config.py`）：

| 常量 | 值 |
|---|---|
| `DATA_DIR` | `~/.agent-smith` |
| `EMPLOYEES_DIR` | `~/.agent-smith/employees` |
| `SQLITE_PATH` | `~/.agent-smith/sqlite/agent-smith.sqlite` |
| `TEMPLATES_DIR` | `<project>/agents/templates` |
| `BUILTIN_SKILLS_DIR` | `<project>/agents/skills` |
| `BUILTIN_TOOLS_DIR` | `<project>/agents/tools` |
| `SAFETY_RULES_PATH` | `<project>/agents/safety/dangerous_commands.json` |
| `BUILTIN_PLUGINS_DIR` | `<project>/agents/plugins` |
| `USER_PLUGINS_DIR` | `~/.agent-smith/plugins` |

**数据库 schema**（`database.py`，SQLite WAL 模式）：

| 表 | 职责 |
|---|---|
| `employees` | Agent 索引（id, name, role, device, online, description, knowledge, config_path） |
| `sessions` | 会话（employee_id 外键） |
| `messages` | 消息（session_id 外键，role 约束 user/assistant/system） |
| `tasks` | 任务（type 约束 conversation/automation，status 四态） |
| `auto_tasks` | 自动任务（trigger_type 三种：manual/cron/interval） |
| `auto_task_runs` | 自动任务执行记录 |

### 2.2 engine/ — 自研 Agent 框架

不知道 FastAPI、不知道 HTTP、不知道 Agent 管理。只做 Agent 执行。设计好以后这一层**不因新增 Agent 而变动**——它定义的是机制，不是内容。

```
engine/
├── execution/                  # 四层执行引擎
│   ├── task_router.py          #   第 1 层：任务路由（规则引擎，不用 LLM）
│   ├── skill_chain.py          #   第 2 层：技能链 DAG 编排（代码控制，确定性）
│   ├── agent_loop.py           #   第 3 层：ReAct 循环（LLM 控制，灵活性）+ 入口
│   ├── gate.py                 #   门禁系统（12 个门禁）
│   └── backtrack.py            #   回退 + Failure Loop Guard
│
├── llm/                        # LLM 调用层
│   ├── client.py               #   OpenAI 兼容 API（httpx, 流式, tool_call 解析, 3 次重试）
│   └── model_config.py         #   从 config_loader 获取 LLM 配置
│
├── prompt/                     # Prompt 组装
│   ├── assembler.py            #   11 层 System Prompt 组装
│   └── placeholder.py          #   占位符渲染 {{name}} {{role}}
│
├── tool/                       # 工具协议 + 注册
│   ├── interface.py            #   ToolProvider 协议
│   ├── registry.py             #   工具注册表：自动发现 agents/tools/ 下的 .py 文件
│   └── schema.py               #   函数签名 → JSON Schema 自动生成
│
├── skill/                      # 技能系统
│   ├── loader.py               #   SKILL.md 解析（YAML frontmatter + Markdown 正文）
│   ├── registry.py             #   技能注册（内置 / 自装 / 市场）
│   ├── executor.py             #   技能执行：加载 → 注入 prompt → 执行
│   └── store.py                #   SkillStore 版本管理（保留最近 10 个版本，支持 rollback）
│
├── memory/                     # 记忆系统
│   ├── interface.py            #   记忆存储协议
│   ├── store.py                #   FileMemoryStore（双作用域：project 优先于 agent）
│   ├── dream.py                #   DreamConsolidator（每 5 次对话触发整理）
│   └── user_learner.py         #   UserPreferenceLearner（4 维偏好自动学习）
│
├── plugin/                     # 插件系统
│   ├── registry.py             #   插件发现
│   ├── trigger.py              #   触发器（Webhook + Polling 两种模式）
│   └── loader.py               #   动态加载
│
├── safety/                     # 安全护栏
│   └── tool_guard.py           #   工具调用前置拦截（24 条正则规则，7 大类）
│
└── pyproject.toml              # 依赖: httpx, pyyaml, aiosqlite
```

#### 2.2.1 四层执行引擎

用户发一条消息后经历四层处理：

```
用户消息："帮我修复登录页的表单验证 bug"
        │
   ┌────▼──────────────────────────────┐
   │ 第 1 层：任务路由（task_router）     │  代码控制
   │ "修复" + "bug" → Bug Fix 路由      │  规则引擎，不用 LLM
   └────┬──────────────────────────────┘
        │
   ┌────▼──────────────────────────────┐
   │ 第 2 层：技能链 DAG（skill_chain）  │  代码控制
   │ sde-debug → planning → testing    │  有向图，强制顺序
   │ → change-validation → code-review │
   └────┬──────────────────────────────┘
        │ 对每个技能节点
   ┌────▼──────────────────────────────┐
   │ 第 3 层：ReAct 循环（agent_loop）   │  LLM 控制
   │ 思考 → 调工具 → 观察 → 思考 → ... │  这里 LLM 有自由度
   └────┬──────────────────────────────┘
        │ 每次调工具前
   ┌────▼──────────────────────────────┐
   │ 第 4 层：安全护栏（tool_guard）     │  代码控制
   │ 正则匹配 24 条规则，拦截危险命令    │  7 类安全规则
   └───────────────────────────────────┘
```

第 1、2、4 层不用 LLM——「先规划再编码」「修 bug 必须先调试」「不能 rm -rf」是确定性规则。LLM 只在第 3 层获得自由。

**三条路由**（`task_router.py`）：

| 输入特征 | 路由结果 | 处理方式 |
|---|---|---|
| 关键词 fix/bug/error + stack trace | `TaskType.BUGFIX` | 走 Bug Fix 技能链 |
| 关键词 feature/add/implement + 需求描述 | `TaskType.FEATURE` | 走 Feature 技能链 |
| 简单问答 / 代码解释 | `TaskType.DIRECT` | 跳过技能链，直接 ReAct |

**技能链定义**（`skill_chain.py`）：

```
Feature 路由：
  planning → architecture(条件:3+文件/跨模块) → testing-strategy → change-validation → code-review

Bug Fix 路由：
  sde-debug → planning → testing-strategy → change-validation → code-review

Direct 路由：
  无技能链，直接进入 agent_loop 的 ReAct 循环
```

硬规则：Bug Fix 必须使用 sde-debug 和 testing-strategy，不可替代；architecture 仅在变更涉及 3+ 文件或跨模块时触发；code-review 始终是最后一步，不可跳过。

#### 2.2.2 门禁系统（`gate.py`）

双层门禁：每个技能执行后先过 Skill Rubric Gate（通用质量检查），再过节点专属 Gate。

| 类型 | 门禁 | 通过条件 |
|---|---|---|
| 流程门禁 | PlanningGate | 步骤 >= 3 + 有验证点 |
| | RootCauseGate | 有根因声明 + 证据支撑 |
| | DesignGate | 涉及文件 + 数据流 + 依赖关系 |
| | TestGate | 测试用例 >= 2 + 边界覆盖 |
| | ValidationGate | 有执行证据 + pass/fail 结果 |
| | ReviewGate | 有 P0/P1 分级 + 可操作发现 |
| 通用门禁 | SkillRubricGate | 长度 > 50 + 代码块/文件引用 + 无错误标记 |
| 交付门禁 | GitWorktreeGate | Worktree 就绪 + 无冲突 |
| | PRGate | Conventional Commit 格式 + 无敏感文件 |
| | TestDeliveryGate | 测试实际运行 + 有 pass/fail 计数 |

#### 2.2.3 回退 + Failure Loop Guard（`backtrack.py`）

```
回退路径：
  Implementation 失败 → 回退到 Planning
  Design 失败       → 回退到 Understand
  Verify 失败       → 回退到 Planning

Failure Loop Guard：
  1. 记录失败签名 = hash(error_type + context_hash)
  2. 同一签名出现 2 次 → 换策略（不是简单重试）
  3. 最多 2 种替代策略 → 全部失败 → blocked，请求人工介入
```

不是限制重试次数，而是识别「用同样方式犯同样错误」。

#### 2.2.4 System Prompt 11 层组装（`assembler.py`）

| 层 | 来源 | 职责 |
|---|---|---|
| 1 | role.md | 核心使命（最高优先级） |
| 2 | style.md | 行为风格 |
| 3 | workflow.md | 工作流 SOP + 门禁定义 |
| 4 | toolbox.md + 工具注册表 | 可用工具声明 + 使用规范 |
| 5 | 技能注册表 | 可用技能摘要 + 使用策略 |
| 6 | expertise.json | 核心能力声明 |
| 7 | traits.json | 工作风格标签 |
| 8 | pipeline.json | 交付承诺（按任务类型定义管线） |
| 9 | context.md | 用户偏好（交互中自动学习填充） |
| 10 | output_style.md | 输出格式规范 |
| 11 | memory/ + 运行时上下文 | 记忆摘要（project 优先于 agent）+ employee_id / session_id / 环境 |

层 1-3 定义「你是谁」，层 4-5 定义「你能做什么」，层 6-8 是结构化元数据，层 9-10 是规范约束，层 11 随时间变化和瞬时运行时信息。

#### 2.2.5 记忆系统

```
~/.agent-smith/employees/<id>/memory/
├── agent/          # Agent 级记忆（跨项目通用）
├── project/        # 项目级记忆（项目特定，搜索时优先）
├── conversations/  # 对话记忆
└── recent.jsonl    # 最近对话索引
```

**Dream 机制**（`dream.py`）：每 5 次对话自动触发 DreamConsolidator：
1. 过滤秘密（8 种模式）
2. 剪枝（30 天无访问）
3. 去重（70% 重叠）
4. 模式提取（3+ 共主题）

**偏好学习**（`user_learner.py`）：4 维启发式检测（语言、简洁度、技术水平、代码风格）。同一特征观察 3 次以上才写入 context.md，仅填充 `{{to_be_learned}}` 占位符，不覆盖用户手动编辑。

#### 2.2.6 技能自进化

| 类型 | 位置 | 权限 |
|---|---|---|
| 内置技能 | `agents/skills/` | 只读 |
| Agent 技能 | `~/.agent-smith/employees/<id>/skills/` | 可进化 |

SkillStore 版本管理：保留最近 10 个版本，支持 rollback 和 diff。skill_manage 工具 7 个操作：list / get / create / edit / patch / versions / rollback。内置技能只读保护。

#### 2.2.7 安全体系

四层安全：

1. **工具调用层** — 24 条正则规则，7 类拦截（command_injection, resource_abuse, code_execution, network_abuse, sensitive_file_access, privilege_escalation, shell_evasion）
2. **文件访问层** — 路径白名单 + 敏感目录黑名单，覆盖 shell、读写、搜索、枚举、Git worktree 等路径型能力
3. **记忆安全层** — 8 种秘密过滤
4. **交付安全层** — PRGate 禁止敏感文件

### 2.3 agents/ — 纯内容层

新增 Agent **只碰这里**。server 启动时自动扫描 `agents/templates/` 发现可用模板。

```
agents/
├── templates/                        # 9 个模板（只读，Git 跟踪）
│   ├── frontend-engineer/            # 每个模板 9 个文件
│   │   ├── role.md                   #   "我是谁" — 使命 / 原则 / 完成标准 / 反目标
│   │   ├── style.md                  #   "我怎么工作" — 风格 / 决策启发 / 好习惯 / 反模式
│   │   ├── workflow.md               #   "我按什么流程交付" — 完整 SOP + 门禁定义
│   │   ├── toolbox.md                #   "我能用什么工具" — 工具清单 + 最佳实践
│   │   ├── context.md                #   用户偏好模板（含 {{to_be_learned}} 占位符）
│   │   ├── config.yaml               #   元信息 + 默认 LLM 配置
│   │   ├── expertise.json            #   核心能力 [{name, description}]
│   │   ├── traits.json               #   工作风格标签
│   │   └── pipeline.json             #   交付承诺（按任务类型定义管线）
│   ├── backend-engineer/
│   ├── product-manager/
│   ├── ui-designer/
│   ├── test-engineer/
│   ├── devops-engineer/
│   ├── data-analyst/
│   └── content-ops/
│
├── skills/                           # 6 个内置技能（所有 Agent 共享，只读）
│   ├── planning/SKILL.md             #   制定实现计划
│   ├── architecture/SKILL.md         #   架构设计
│   ├── code-review/SKILL.md          #   代码审查
│   ├── sde-debug/SKILL.md            #   根因调试
│   ├── testing-strategy/SKILL.md     #   测试策略
│   └── change-validation/SKILL.md    #   变更验证
│
├── tools/                            # 9 个内置工具 provider（所有 Agent 共享）
│   ├── read_file.py                  #   文件读取
│   ├── write_file.py                 #   文件写入
│   ├── shell.py                      #   Shell 命令执行
│   ├── git_ops.py                    #   Git 操作
│   ├── memory_ops.py                 #   记忆操作
│   ├── search_knowledge.py           #   知识库检索
│   ├── web_fetch.py                  #   网页抓取
│   ├── skill_load.py                 #   技能加载
│   └── skill_manage.py               #   技能管理（7 个操作）
│
├── plugins/                          # 插件
│   └── github/                       #   GitHub webhook 插件
│
├── safety/                           # 安全规则
│   └── dangerous_commands.json       #   24 条规则，7 大类
│
└── output_style.md                   # 输出格式规范
```

**SKILL.md 格式规范**（YAML frontmatter + Markdown 正文）：

```yaml
---
name: planning
description: "制定实现计划，输出步骤、验证点和风险项"
argument-hint: "可选参数提示"
version: "1.0.0"
---
# Planning

## 目标
...

## 步骤
1. [步骤] → 验证: [检查点]

## 输出格式
...
```

### 2.4 server/ — 平台后端 FastAPI

```
server/
└── app/
    ├── main.py                     # FastAPI 入口
    │
    ├── domain/                     # 领域模型（7 个）
    │   ├── employee.py             #   Employee
    │   ├── session.py              #   Session
    │   ├── task.py                 #   Task
    │   ├── auto_task.py            #   AutoTask
    │   ├── team.py                 #   Team
    │   ├── auth.py                 #   Auth
    │   └── __init__.py
    │
    ├── routers/                    # 12 个路由模块（薄壳）
    │   ├── employees.py            #   Agent CRUD
    │   ├── sessions.py             #   会话管理
    │   ├── tasks.py                #   任务管理
    │   ├── auto_tasks.py           #   自动任务
    │   ├── templates.py            #   模板列表
    │   ├── teams.py                #   团队协作（@mention 路由）
    │   ├── config.py               #   配置管理
    │   ├── files.py                #   文件操作
    │   ├── stats.py                #   统计数据
    │   ├── plugins.py              #   插件管理
    │   ├── auth.py                 #   认证
    │   └── __init__.py
    │
    ├── services/                   # 11 个业务服务（编排层）
    │   ├── employee_service.py     #   Agent 生命周期
    │   ├── session_service.py      #   会话 + Agent 执行
    │   ├── task_service.py         #   任务管理
    │   ├── auto_task_service.py    #   自动任务调度
    │   ├── template_service.py     #   模板扫描
    │   ├── team_service.py         #   多 Agent 协作（@mention 分发）
    │   ├── config_service.py       #   配置读写
    │   ├── stats_service.py        #   统计计算
    │   ├── plugin_service.py       #   插件生命周期
    │   └── scheduler.py            #   cron 调度器
    │
    ├── infrastructure/             # Repository 持久化
    │   └── repositories/
    │       ├── employee_repo.py    #   Agent CRUD
    │       ├── session_repo.py     #   会话 CRUD
    │       ├── task_repo.py        #   任务 CRUD
    │       ├── auto_task_repo.py   #   自动任务 CRUD
    │       └── team_repo.py        #   团队 CRUD
    │
    └── utils/
        └── cron.py                 #   cron 表达式解析
```

**Router 层规范**：Router 只做参数提取 → 调 service → 返回结果。不做业务判断、数据查找、条件分支。业务逻辑全在 service 层。

**多 Agent 协作**：用户消息中 `@employee_id` 自动分发给对应 Agent，每个 Agent 的 prompt 包含团队对话上下文。API: `POST /api/teams/{id}/messages`（同步）、`POST /api/teams/{id}/messages/stream`（SSE 流式）。

### 2.5 app/ — SwiftUI macOS 原生前端

| 维度 | 选型 |
|---|---|
| 语言 | Swift 6.1 |
| 框架 | SwiftUI |
| 最低版本 | macOS 15+ |
| 包管理 | SPM (Swift Package Manager) |
| 第三方依赖 | 零 |
| 通信 | URLSession（HTTP + SSE） |

```
app/AgentSmith/Sources/AgentSmith/
├── AgentSmithApp.swift                     # 应用入口
├── ContentView.swift                       # 主内容视图
├── WindowChromeConfigurator.swift          # 窗口样式配置
│
├── Components/                             # 设计系统 + 公共组件
│   ├── AppPalette.swift                    #   5 个语义色
│   ├── AppTypography.swift                 #   3 档字号
│   ├── EmployeePortraitView.swift          #   Agent 头像组件
│   ├── WorkRecordView.swift                #   工作记录组件
│   ├── MemoryTimelineView.swift            #   记忆时间线组件
│   └── SidebarMaterialView.swift           #   侧边栏材质组件
│
├── Models/
│   └── Employee.swift                      #   Agent 数据模型
│
├── Services/
│   └── APIClient.swift                     #   HTTP/SSE 通信
│
└── Views/
    ├── Management/                         #   Agent 管理
    │   ├── ManagementView.swift            #     Agent 列表主视图
    │   ├── EmployeeCardView.swift          #     Agent 卡片
    │   └── CreateEmployeeSheet.swift       #     新建 Agent 弹窗
    │
    ├── Employee/                           #   Agent 详情
    │   ├── EmployeeDetailView.swift        #     详情主视图（9 标签页导航）
    │   ├── EmployeeHomeView.swift          #     首页（身份卡 + 工作记录）
    │   ├── EmployeeTasksView.swift         #     任务列表
    │   ├── EmployeeAutomationsView.swift   #     自动任务
    │   ├── EmployeeMemoryView.swift        #     记忆
    │   ├── EmployeeSkillsView.swift        #     技能
    │   ├── EmployeeConnectorsView.swift    #     连接器
    │   └── EmployeePermissionsView.swift   #     权限
    │
    ├── Conversation/
    │   └── ConversationView.swift          #     对话任务
    │
    └── Settings/
        └── SettingsView.swift              #     设置页
```

**设计系统**：

- `AppPalette` — 5 个语义色，支持 Light/Dark 模式自动切换
- `AppTypography` — 3 档字号（标题、正文、辅助）
- 状态管理：`@State` + `@AppStorage` + `@EnvironmentObject`

---

## 三、LLM 配置四层覆盖

| 层级 | 位置 | 优先级 | 场景 |
|---|---|---|---|
| 环境变量/平台级 | 环境变量 `AGENTSMITH_LLM_*` + `~/.agent-smith/config.yaml` | 最低 | 全局默认 |
| 模板级 | `agents/templates/<role>/config.yaml` | ↑ | 模板建议的默认模型 |
| Agent 级 | `~/.agent-smith/employees/<id>/config.yaml` | ↑ | 该 Agent 专属配置 |
| 会话级 | API 参数传入 | 最高 | 临时切换模型 |

解析逻辑（`config_loader.py`）：从上到下逐层 deep merge，下层**覆盖**上层已有字段，未填字段**继承**上层。

支持的环境变量：

| 环境变量 | 配置字段 |
|---|---|
| `AGENTSMITH_LLM_API_KEY` | `api_key` |
| `AGENTSMITH_LLM_BASE_URL` | `base_url` |
| `AGENTSMITH_LLM_MODEL` | `model` |
| `AGENTSMITH_LLM_PROVIDER` | `provider` |

配置示例：

```yaml
# ~/.agent-smith/config.yaml（平台级）
llm:
  provider: openai
  api_key: sk-xxx
  base_url: https://newapi.elevatesphere.com/v1
  model: glm-4.7

# ~/.agent-smith/employees/<id>/config.yaml（Agent 级）
llm:
  model: gpt-4o          # 只覆盖模型，key/url 继承平台级
tools:
  enabled: [read_file, write_file, shell]
  disabled: [web_fetch]  # 这个 Agent 不需要抓网页
```

---

## 四、模板 → 实例生命周期

```
代码仓库（只读）                          用户运行时数据
──────────────                          ────────────────

agents/templates/frontend-engineer/     ~/.agent-smith/employees/<id>/
├── role.md         ─── 创建时复制 ───→  ├── role.md            可编辑
├── style.md        ─── 创建时复制 ───→  ├── style.md           可编辑
├── workflow.md     ─── 创建时复制 ───→  ├── workflow.md        可编辑
├── toolbox.md      ─── 创建时复制 ───→  ├── toolbox.md         可编辑
├── context.md      ─── 创建时复制 ───→  ├── context.md         自动学习填充
├── config.yaml     ─── 创建时复制 ───→  ├── config.yaml        可覆盖 LLM
├── expertise.json  ─── 创建时复制 ───→  ├── expertise.json     可编辑
├── traits.json     ─── 创建时复制 ───→  ├── traits.json        可编辑
├── pipeline.json   ─── 创建时复制 ───→  ├── pipeline.json      可编辑
│                                       ├── memory/            运行时积累
Git 跟踪，永不修改                        ├── skills/            可自装技能
                                        └── sessions/          会话数据
                                        .gitignore，不入库
```

创建后独立。用户改实例不影响模板，模板更新不影响已有实例。

---

## 五、运行时数据目录

```
~/.agent-smith/
├── config.yaml                          # 平台级配置（LLM key/url/model）
├── employees/
│   └── <id>/                            # 实例化后的 Agent
│       ├── role.md                      # 从模板复制，可编辑
│       ├── style.md
│       ├── workflow.md
│       ├── toolbox.md
│       ├── context.md                   # 交互中自动学习填充
│       ├── config.yaml                  # Agent 级配置
│       ├── expertise.json
│       ├── traits.json
│       ├── pipeline.json
│       ├── memory/                      # 运行时记忆
│       │   ├── agent/                   # Agent 级（跨项目通用）
│       │   ├── project/                 # 项目级（项目特定，搜索优先）
│       │   ├── conversations/           # 对话记忆
│       │   └── recent.jsonl             # 最近对话索引
│       ├── skills/                      # Agent 自装技能（可自进化）
│       └── sessions/                    # 会话数据
├── plugins/                             # 用户插件
└── sqlite/
    └── agent-smith.sqlite               # 索引库（WAL 模式，6 表）
```

---

## 六、变更影响矩阵

| 操作 | common | engine | agents | server | app |
|---|---|---|---|---|---|
| 新加内置 Agent | | | **agents/templates/** | | |
| 新加内置技能 | | | **agents/skills/** | | |
| 新加内置工具 | | | **agents/tools/** | | |
| 改执行引擎逻辑 | | **engine/** | | | |
| 加 API 端点 | | | | **server/routers/** + **services/** | |
| 换存储方案 | **common/** | | | | |
| 加 UI 页面 | | | | | **app/** |
| 改 LLM 配置逻辑 | **common/config_loader.py** | | | | |
| 用户新建/改 Agent | -- | -- | -- | -- | -- |

最后一行是运行时操作，只改 `~/.agent-smith/employees/`，不碰任何代码。

---

## 七、插件系统

```
engine/plugin/     ← 框架（不知道 FastAPI）
agents/plugins/    ← 内容（plugin.json + handler.py + skills/）
server/routers/    ← HTTP 入口（webhook 接收）
server/services/   ← 编排
```

两种触发模式：

| 模式 | 场景 | 示例 |
|---|---|---|
| Webhook | 外部事件推送 | GitHub Issue/PR 事件 |
| Polling | 定时轮询外部源 | 定时检查外部数据源 |

---

## 八、完整执行流程示例

用户给前端工程师发消息：「帮我修复登录页的表单验证 bug」

```
 1. [task_router] 关键词"修复""bug" → Bug Fix 路由
 2. [skill_chain] 加载 Bug Fix 技能链：
    sde-debug → planning → testing-strategy → change-validation → code-review
 3. [sde-debug] ReAct 循环 → 根因分析 → SkillRubricGate + RootCauseGate 通过
 4. [planning] ReAct 循环 → 修复计划 → SkillRubricGate + PlanningGate 通过
 5. [testing-strategy] ReAct 循环 → 测试策略 → SkillRubricGate + TestGate 通过
 6. [change-validation] ReAct 循环 → 执行修复 + 运行测试
    → ValidationGate 失败 → backtrack 到 planning
    → Failure Loop Guard: 第 1 次，常规回退
    → 重新 planning → 重新 validation → 通过
 7. [code-review] ReAct 循环 → 自审 → SkillRubricGate + ReviewGate 通过
 8. save_conversation_memory()
 9. UserPreferenceLearner.observe()
10. 每 5 次 → Dream 整理记忆
11. 返回结果
```

---

## 九、借鉴来源

| 来源 | 借鉴内容 | 不借鉴内容 |
|---|---|---|
| QoderWake | 执行模型（路由/技能链/门禁/回退）、人格文件系统、SKILL.md 格式、安全护栏、记忆系统、Prompt 组装 | TypeScript/Bun 运行时、品牌资产、具体实现代码 |

---

## 十、文件映射总览

```
Agent-Smith/
├── common/                         # 基础设施
│   ├── config.py                   # 路径常量（10 个）
│   ├── config_loader.py            # 四层 LLM 配置合并
│   ├── database.py                 # SQLite WAL（6 表）
│   ├── filesystem.py               # 文件工具
│   ├── log.py                      # 日志
│   └── yaml_utils.py               # YAML 解析
│
├── engine/                         # 执行框架
│   ├── execution/
│   │   ├── task_router.py          # 第 1 层：任务路由（规则引擎）
│   │   ├── skill_chain.py          # 第 2 层：技能链 DAG
│   │   ├── agent_loop.py           # 第 3 层：ReAct 循环 + 入口
│   │   ├── gate.py                 # 12 个门禁
│   │   └── backtrack.py            # 回退 + Failure Loop Guard
│   ├── llm/
│   │   ├── client.py               # LLM API（httpx, 流式, tool_call, 3 次重试）
│   │   └── model_config.py         # 配置获取
│   ├── prompt/
│   │   ├── assembler.py            # 11 层 Prompt 组装
│   │   └── placeholder.py          # 占位符渲染
│   ├── tool/
│   │   ├── interface.py            # ToolProvider 协议
│   │   ├── registry.py             # 工具注册（auto-discover .py）
│   │   └── schema.py               # JSON Schema 生成
│   ├── skill/
│   │   ├── loader.py               # SKILL.md 解析
│   │   ├── registry.py             # 技能注册（内置 / 自装）
│   │   ├── executor.py             # 技能执行
│   │   └── store.py                # 版本管理（10 版本）
│   ├── memory/
│   │   ├── interface.py            # 记忆协议
│   │   ├── store.py                # FileMemoryStore（双作用域）
│   │   ├── dream.py                # DreamConsolidator
│   │   └── user_learner.py         # UserPreferenceLearner
│   ├── plugin/
│   │   ├── registry.py             # 插件发现
│   │   ├── trigger.py              # 触发器（Webhook + Polling）
│   │   └── loader.py               # 动态加载
│   └── safety/
│       └── tool_guard.py           # 安全拦截（24 条规则，7 类）
│
├── agents/                         # Agent 内容
│   ├── templates/                  # 9 个模板（每个 9 文件）
│   ├── skills/                     # 6 个内置技能
│   ├── tools/                      # 9 个工具 provider
│   ├── plugins/                    # 插件（GitHub webhook）
│   ├── safety/                     # 安全规则
│   └── output_style.md             # 输出格式
│
├── server/                         # 平台后端
│   └── app/
│       ├── main.py                 # FastAPI 入口
│       ├── domain/                 # 领域模型（7 个）
│       ├── routers/                # 12 个路由
│       ├── services/               # 11 个业务服务
│       ├── infrastructure/         # Repository 持久化（5 个 repo）
│       └── utils/                  # cron 解析
│
├── app/                      # macOS 前端（SwiftUI, Swift 6.1, macOS 15+）
└── docs/                           # 设计文档
```
